虚拟机可以正常挂载，容器内挂载失败。
1。网络防火墙策略导致
2。NAS存储机制，抓包分析：
虚拟机执行showmount –e 时，可以返回正常结果，容器执行showmount时返回结果为空。
经过抓包分析，发现两者在发送请求是的UID 和GID 不一致，虚拟机执行时GID 和UID 为0.
容器内执行时发送请求带的UID 和GID 是1000。容器内是非root用户。
怀疑是导致返回结果不一致的原因，需要EMC厂商确认下原因，以及这种是否可调整，
